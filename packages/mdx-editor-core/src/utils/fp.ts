// vendored from @mdxeditor/editor v3.55.0 src/utils/fp.ts — MIT © Petyo Ivanov
// Only `noop` is vendored; the remaining functional helpers from upstream are not used here.

/**
 * A no-op function.
 * @internal
 */
export function noop() {
  // intentionally empty
}
